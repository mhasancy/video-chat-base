{
  "name": "get-token",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "ngrok": "^4.3.1"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  }
}
